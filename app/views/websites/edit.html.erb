<div class="container mt-5">
  <div class="card shadow-lg border-0 rounded-4 bg-white">
    <div class="card-header bg-primary text-white text-center py-3 rounded-top-4">
      <h2 class="fw-bold">Update Website Information</h2>
    </div>
    <div class="card-body p-4">
      <%= render 'form', website: @website %>

      <div class="d-flex justify-content-center mt-4 gap-3">
        <%= link_to 'Back to List', websites_path, class: "btn btn-outline-secondary px-4 py-2 fw-bold rounded-pill" %>
        <%= link_to 'Home', root_path, class: "btn btn-primary px-4 py-2 fw-bold rounded-pill" %>
      </div>
    </div>
  </div>
</div>
